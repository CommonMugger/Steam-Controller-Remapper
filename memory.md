# Project Memory

- After app changes, close any running `Steam Controller Remapper` instance, rebuild the `Release` target, and relaunch the updated release executable.
- Keep all changes machine-agnostic. Do not hardcode behavior around one specific PC, drive layout, or local environment when working on this project.
